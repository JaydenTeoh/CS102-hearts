import java.util.*;

public class Hand {
    private ArrayList<Card> cards;

    public ArrayList<Card> getCards() {
        return cards;
    }

    // idk if we need this? im a bit confused because player has getHand and setHand already
    // does this mean that player is modifying this object
    // public void setCards(ArrayList<Card> cards) {
    //     this.cards = cards;
    // }

    
}
